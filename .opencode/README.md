# OpenCode Config

Skill discovery path:

- `.opencode/skills/<name>/SKILL.md`

This repository intentionally ships one integrated skill:

- `.opencode/skills/diasync-memory/`

Runtime memory is managed in hidden `.memory/` by `.opencode/skills/diasync-memory/scripts/memoryctl.py`.
